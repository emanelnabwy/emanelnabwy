pipeline {
    agent any
    stages {
        stage("build") {
           steps {
              echo " application is building ......" 
           }   
         }
        stage("test") {
            steps {
               echo " application is testing ......"
            }    
         }
        stage("deploy") {
            steps { 
                echo " application is deploy ......"
            }    
         }
    }
}
